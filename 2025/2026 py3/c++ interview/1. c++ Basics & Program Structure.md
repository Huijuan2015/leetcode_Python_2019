以下是为您整理的 C++ 基础与程序结构面试教程，保留了英文原文并在每段后附带了中文翻译。

---

### **C++ Basics & Program Structure Interview Questions**
**C++ 基础与程序结构面试题**
*Last Updated : 27 Aug, 2025*
*最后更新日期：2025年8月27日*

C++ is a structured and object-oriented language used in systems programming, game development, competitive coding.
C++ 是一种结构化且面向对象的语言，常用于系统编程、游戏开发和算法竞赛。

---

#### **1. How does C++ enable both high-level and low-level programming, and what features make it suitable for systems-level development?**
**1. C++ 是如何同时实现高级和低级编程的？哪些特性使其适合系统级开发？**

C++ is called a middle-level language because it combines the features of both high-level and low-level programming.
C++ 被称为中级语言，因为它结合了高级编程和低级编程的特点。

* **High-level features:** It supports abstraction, object-oriented programming, templates, and the standard library, which make writing complex applications easier and closer to human thinking.
    **高级特性：** 它支持抽象、面向对象编程、模板和标准库，这使得编写复杂的应用程序更加容易，且更接近人类的思维方式。
* **Low-level features:** It allows direct memory management using pointers, inline assembly, and hardware-level manipulation, which makes it suitable for systems programming like operating systems, compilers, and device drivers.
    **低级特性：** 它允许使用指针进行直接内存管理、内联汇编和硬件级操作，这使其适用于操作系统、编译器和设备驱动程序等系统编程。

**Reasons why C++ is suitable for systems-level development:**
**C++ 适合系统级开发的原因：**
* **Direct Memory Access:** Through pointers and pointer arithmetic.
    **直接内存访问：** 通过指针和指针算术实现。
* **Manual Resource Management:** Developers can control memory allocation/deallocation using `new` and `delete`.
    **手动资源管理：** 开发人员可以使用 `new` 和 `delete` 控制内存的分配和释放。
* **Performance:** Minimal runtime overhead compared to other higher-level languages.
    **性能：** 与其他更高级别的语言相比，运行时的额外开销极小。
* **Object-Oriented Design:** Helps in organizing large system codebases (e.g., OS kernels, databases).
    **面向对象设计：** 有助于组织大型系统代码库（例如操作系统内核、数据库）。

```cpp
#include <iostream>
using namespace std;

int main() {
    int x = 10;          // high-level: simple variable
    int* p = &x;         // low-level: direct memory access with pointer

    *p = 20;             // modifying value through pointer
    cout << "x = " << x << endl;

    return 0;
}
```

* **High-level:** `int x = 10;` shows simple variable handling.
    **高级：** `int x = 10;` 展示了简单的变量处理。
* **Low-level:** `int* p = &x;` and `*p = 20;` show direct memory manipulation.
    **低级：** `int* p = &x;` 和 `*p = 20;` 展示了直接的内存操作。
* It clearly demonstrates how C++ can act at both levels in just a few lines.
    这清楚地证明了 C++ 如何在短短几行代码中同时发挥两个层面的作用。

---

#### **2. What is 'namespace std'?**
**2. 什么是 'namespace std'？**

'std' stands for Standard. It is a namespace in C++. Namespace is a feature that provides a way to group related identifiers such as variables, functions, and classes under a single name.
'std' 代表标准（Standard）。它是 C++ 中的一个命名空间。命名空间是一种将相关的标识符（如变量、函数和类）分组在一个名称下的特性。

The line "using namespace std" informs the compiler to add everything under the std namespace and inculcate them in the global namespace. This all inculcation of global namespace benefits us to use "cout" and "cin" without using "std::".
“using namespace std” 这行代码通知编译器将 std 命名空间下的所有内容添加到全局命名空间中。这种引入全局命名空间的好处是使我们能够直接使用 “cout” 和 “cin”，而无需加上 “std::” 前缀。

```cpp
#include <iostream>
using namespace std;

int main() {
    cout << "Hello using std namespace!" << endl;
    return 0;
}
```

---

#### **3. What are references in C++?**
**3. C++ 中的引用是什么？**

In C++, references is a way to create an alias for another variable.
在 C++ 中，引用是为另一个变量创建别名的一种方式。

* A reference acts as a synonym for a variable, allowing you to access the variable directly without any additional syntax.
    引用充当变量的同义词，允许你直接访问该变量而无需任何额外的语法。
* They must be initialized when created and cannot be changed to refer to another variable afterward.
    它们在创建时必须被初始化，并且之后不能更改为引用另一个变量。
* This feature makes it easier to manipulate variables in functions while avoiding the overhead of copying large objects. A reference variable is preceded with a '&' symbol.
    这一特性使得在函数中操作变量更加容易，同时避免了复制大型对象的开销。引用变量以 '&' 符号开头。

**Syntax:**
**语法：**
```cpp
int GFG = 10;
// reference variable
int& ref = GFG;
```

```cpp
#include <iostream>
using namespace std;

int main() {
    int a = 10;
    int &ref = a;

    ref = 20;
    cout << "a = " << a << ", ref = " << ref << endl;
    return 0;
}
```

---

#### **4. Define the Block scope variable.**
**4. 定义块级作用域变量。**

A block scope variable is also known as a local scope variable. A variable that is defined inside a function (like main) or inside a block (anything enclosed with curly braces like loops and if else) is a local variable. A block-scoped variable will not be available outside the block even if the block is inside a function.
块级作用域变量也称为局部作用域变量。在函数（如 main）内部或代码块（任何由大括号包围的内容，如循环和 if-else 语句）内部定义的变量就是局部变量。即使该代码块位于函数内部，块级作用域变量在代码块外部也是不可用的。

```cpp
#include <iostream>
using namespace std;

int main() {
    int a = 10; // local to main

    if (a > 5) {
        int b = 20;  // block scope inside if-block
        cout << "Inside if-block: a = " << a << ", b = " << b << endl;
    }
    for (int i = 0; i < 3; i++) {
        int temp = i * 10;  // block scope inside for-loop
        cout << "temp = " << temp << endl;
    }

    return 0;
}
```

---

#### **5. What does the "auto" keyword do?**
**5. "auto" 关键字的作用是什么？**

The auto keyword is a type inference specifier introduced in C++11, which tells the compiler to automatically deduce the type of a variable from its initializer expression.
auto 关键字是 C++11 引入的一种类型推导说明符，它告诉编译器根据变量的初始化表达式自动推导其类型。

**Primary Function of auto:**
**auto 的主要功能：**
* Simplifies declarations (especially for iterators, lambdas, template types).
    简化声明（特别是对于迭代器、lambda 表达式、模板类型）。
* Enhances code readability and maintainability.
    提高代码的可读性和可维护性。
* Reduces redundancy and typing errors.
    减少冗余和输入错误。
* Promotes use of generic programming.
    促进泛型编程的使用。

**Use Case: With Iterators**
**用法示例：配合迭代器使用**

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    vector<int> v = {1, 2, 3, 4};
    for (auto it = v.begin(); it != v.end(); ++it)
        cout << *it << " ";

    return 0;
}
```
**Instead of this:**
**代替以下写法：**
`std::vector<int>::iterator it = v.begin();`

---

#### **6. What do you mean by Pass by Value and Pass by Reference?**
**6. 什么是按值传递和按引用传递？**

In this programming language to call a function we have 2 methods: Pass by Value and Pass by Reference.
在这种编程语言中，调用函数有两种方法：按值传递和按引用传递。

| **Pass by Value (按值传递)** | **Pass by Reference (按引用传递)** |
| :--- | :--- |
| A copy of a variable is passed. | Either the address of variable or reference to the variable is passed. |
| 传递的是变量的副本。 | 传递的是变量的地址或变量的引用。 |
| The changes made in the function are never reflected outside the function on the variable. In short, the original value is never altered. | The changes made in the functions can be seen outside the function. In short, the original value is altered. |
| 在函数中所做的更改永远不会反映在函数外部的变量上。简而言之，原始值永远不会被改变。 | 在函数中所做的更改可以在函数外部看到。简而言之，原始值会被改变。 |
| Passed actual and formal parameters are stored in different memory locations, making it a little memory inefficient. | Passed actual and formal parameters are stored in the same memory location, making it more memory efficient. |
| 传递的实参和形参存储在不同的内存位置，因此内存效率稍低。 | 传递的实参和形参存储在相同的内存位置，因此内存效率更高。 |

---

#### **7. Define token in C++**
**7. 定义 C++ 中的标记 (Token)**

A token is the smallest individual element of a program that is understood by a compiler. A token comprises the following:
标记（Token）是程序中能被编译器理解的最小独立元素。一个标记包含以下内容：

* **Keywords:** That contain a special meaning to the compiler.
    **关键字：** 对编译器具有特殊含义的词。
* **Identifiers:** That hold a unique value/identity.
    **标识符：** 具有唯一值或身份的名称。
* **Constants:** That never change their value throughout the program.
    **常量：** 在整个程序运行期间其值永远不会改变。
* **Strings:** That contains the homogenous sequence of data.
    **字符串：** 包含同类数据序列的内容。
* **Special Symbols:** They have some special meaning and cannot be used for another purpose; eg: `[] () {}, ; * = #`.
    **特殊符号：** 它们具有某些特殊含义，不能用于其他目的；例如：`[] () {}, ; * = #`。
* **Operators:** Who perform operations on the operand.
    **运算符：** 对操作数执行运算。

```cpp
#include <iostream>
using namespace std; // Keyword

int main() {
    int age = 25;       // int (keyword), age (identifier), 25 (constant)
    cout << "Age: " << age << endl; // "Age:" (string), << (operator)
    return 0;           // return (keyword), 0 (constant)
}
```

---

#### **8. What is the difference between C and C++?**
**8. C 和 C++ 有什么区别？**

| **C** | **C++** |
| :--- | :--- |
| C language came before C++ and has a subset of features provided by C++. | C++ can be seen as a superset of C. Almost everything written in C compiles in C++ as well. |
| C 语言早于 C++，它拥有 C++ 所提供功能的一个子集。 | C++ 可以被看作是 C 的超集。几乎所有用 C 编写的内容也可以在 C++ 中编译。 |
| It is a procedural programming language. It does not support classes and objects. | It supports both procedural and object-oriented programming languages. |
| 它是一种过程化编程语言。它不支持类和对象。 | 它同时支持过程化和面向对象编程语言。 |
| It does not support any OOP concepts like polymorphism, abstraction, encapsulation. | It supports all OOP concepts like classes, objects, inheritance, polymorphism, etc. |
| 它不支持任何 OOP 概念，如多态、抽象、封装。 | 它支持所有 OOP 概念，如类、对象、继承、多态等。 |
| It does not support Exception Handling and Generics. | Supports Exception Handling and Generics through templates. |
| 它不支持异常处理和泛型。 | 通过模板支持异常处理和泛型。 |
| It is a function-driven language. | It is an object-driven language. |
| 它是以函数驱动的语言。 | 它是以对象驱动的语言。 |

---

#### **9. What happens if you use cout without using namespace std or std::cout?**
**9. 如果你在不使用 namespace std 或 std::cout 的情况下使用 cout 会发生什么？**

In C++, cout belongs to the std namespace. If you forget to use `using namespace std;` or `std::cout`, you'll get a compiler error. In large projects, polluting the global namespace can lead to naming collisions.
在 C++ 中，cout 属于 std 命名空间。如果你忘记使用 `using namespace std;` 或 `std::cout`，你会收到编译错误。在大型项目中，污染全局命名空间可能会导致命名冲突。

---

#### **10. What is the difference between const and #define?**
**10. const 和 #define 有什么区别？**

* **const:** A typed constant with scope rules, checked by the compiler.
    **const：** 具有类型和作用域规则的常量，由编译器检查。
* **#define:** A preprocessor macro replaced before compilation, no type checking.
    **#define：** 在编译前进行替换的预处理器宏，没有类型检查。

---

#### **11. What are default arguments?**
**11. 什么是默认参数？**

Default arguments are values that are used when a function is called without some parameters.
默认参数是在调用函数时如果没有提供某些参数则使用的值。

```cpp
#include <iostream>
using namespace std;

void greet(string name = "Guest") {
    cout << "Hello " << name;
}

int main() {
    greet(); // Output: Hello Guest
    return 0;
}
```

---

#### **12. What are type modifiers in C++?**
**12. C++ 中的类型修饰符是什么？**

Modifiers like signed, unsigned, long, short change the size or sign of basic data types.
像 signed、unsigned、long、short 这样的修饰符会改变基本数据类型的大小或符号。

---

#### **13. How does type deduction using auto and decltype in C++ enhance type safety and flexibility?**
**13. C++ 中使用 auto 和 decltype 的类型推导如何增强类型安全性和灵活性？**

Instead of manually specifying data types, C++ allows the compiler to deduce the type using:
C++ 允许编译器通过以下方式推导类型，而不是手动指定数据类型：
* **auto:** Deduces the type from initializer.
    **auto：** 从初始化器推导类型。
* **decltype:** Infers type from an expression.
    **decltype：** 从表达式推导类型。

These features reduce boilerplate, improve maintainability, and prevent type mismatches.
这些特性减少了样板代码，提高了可维护性，并防止了类型不匹配。

---

#### **14. What is a reference in C++ and how is it different from a pointer?**
**14. C++ 中的引用是什么？它与指针有何不同？**

* A reference is an alias for another variable. Once initialized, it always refers to the same variable.
    引用是另一个变量的别名。一旦初始化，它始终指向同一个变量。
* A pointer stores the address of a variable and can be changed to point somewhere else.
    指针存储变量的地址，并且可以更改为指向其他地方。
* References do not need dereferencing (*) to access the value; they behave just like the original variable.
    引用不需要解引用（*）来访问值；它们的行为就像原始变量一样。

---

#### **15. Can a reference be reseated to another variable after initialization? What happens if you try to return a reference to a local variable?**
**15. 引用在初始化后可以重新指向另一个变量吗？如果尝试返回对局部变量的引用会发生什么？**

* References cannot be reseated.
    引用不能重新指向。
* Returning a reference to a local variable causes undefined behavior — you're returning a variable that no longer exists after function ends.
    返回对局部变量的引用会导致未定义行为——你返回的是一个在函数结束后就不再存在的变量。

---

#### **16. Discuss the difference between prefix and postfix?**
**16. 讨论前缀（prefix）和后缀（postfix）的区别？**

| **Prefix (++i)** | **Postfix (i++)** |
| :--- | :--- |
| Operator before the operand. | Operator after the operand. |
| 运算符在操作数之前。 | 运算符在操作数之后。 |
| The variable is incremented first, and then the updated value is used in the expression. | The current value of the variable is used in the expression first, and then the increment happens afterward. |
| 变量先递增，然后在表达式中使用更新后的值。 | 先在表达式中使用变量的当前值，然后才发生递增。 |
| Associativity is right to left. | Associativity is left to right. |
| 结合性是从右到左。 | 结合性是从左到右。 |

---

#### **17. Define namespace in C++.**
**17. 定义 C++ 中的命名空间。**

Namespaces enable us to organize named items into smaller scopes. This permits program parts to be organized into distinct logical scopes with names. It avoids name clashes, which might happen when you have many libraries in your code base.
命名空间使我们能够将命名项组织到更小的作用域中。这允许程序的各个部分被组织到具有名称的、不同的逻辑作用域中。它可以避免命名冲突，当你的代码库中有很多库时，这种冲突可能会发生。

---

#### **18. When is void() return type used?**
**18. 什么时候使用 void() 返回类型？**

The `void` keyword, when used as a function return type, indicates that the function does not return a value. You can use `return;` in such functions, but you cannot return an actual value.
`void` 关键字用作函数返回类型时，表示该函数不返回值。你可以在此类函数中使用 `return;`，但不能返回实际的值。

---

#### **19. What is the difference between a reference and a pointer?**
**19. 引用和指针的区别是什么？**

* **Reference:** An alias for a variable. It cannot be NULL and must be initialized.
    **引用：** 变量的别名。它不能为 NULL 且必须初始化。
* **Pointer:** A variable storing an address. It can be reassigned and can be NULL.
    **指针：** 存储地址的变量。它可以被重新赋值，也可以为 NULL。

---

#### **20. In the line "int* a, b;", how many pointers are declared?**
**20. 在 "int* a, b;" 这行代码中，声明了多少个指针？**

Only `a` is a pointer — `b` is just an `int`.
只有 `a` 是指针 —— `b` 只是一个普通的 `int`。

---

#### **21. Define storage class in C++ and name some.**
**21. 定义 C++ 中的存储类并列举一些。**

Storage class is used to define the scope (visibility), lifetime, and linkage of variables or functions.
存储类用于定义变量或函数的范围（可见性）、生命周期和链接性。
* **Examples:** `auto`, `register`, `static`, `extern`, `mutable`.
    **示例：** `auto`、`register`、`static`、`extern`、`mutable`。

---

#### **22. What is a mutable storage class specifier? How can they be used?**
**22. 什么是可变（mutable）存储类说明符？如何使用它们？**

The `mutable` keyword is a storage class specifier used only with non-static data members of a class. It allows a member of a `const` object to be modified.
`mutable` 关键字是一种存储类说明符，仅用于类的非静态数据成员。它允许修改 `const` 对象的成员。

* **Why is it needed:** Sometimes you want to modify an internal variable (e.g., access counter, log flag) that is not part of the logical state of the object, even inside a `const` member function.
    **为什么需要它：** 有时你想修改一个不属于对象逻辑状态的内部变量（例如访问计数器、日志标志），即使是在 `const` 成员函数内部也是如此。

```cpp
class Logger {
private:
    mutable int accessCount; // can change even in const functions
    string data;
public:
    void showData() const {
        accessCount++;  // allowed because accessCount is mutable
        cout << "Data: " << data << ", Accessed: " << accessCount << " times\n";
    }
};
```
Even though the object might be `const`, `accessCount` can still be updated.
即使对象被声明为 `const`，`accessCount` 仍然可以更新。